import { LoopTimesDirective } from './loop-times.directive';

describe('LoopTimesDirective', () => {
  it('should create an instance', () => {
    const directive = new LoopTimesDirective();
    expect(directive).toBeTruthy();
  });
});
